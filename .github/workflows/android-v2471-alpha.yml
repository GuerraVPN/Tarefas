name: Android 2.4.7.1 Alpha - somente patch 2.4.6.8

on:
  workflow_dispatch:
  push:
    branches: [app/android-v2471-alpha-prep]
  pull_request:
    branches: [main]
    paths:
      - 'app/android/TarefasLauncherIconPlugin.java'
      - 'app/mobile-launcher-icon-v241.js'
      - 'app/release-v2471.txt'
      - 'scripts/build-mobile-v246.mjs'
      - 'scripts/build-mobile-v2471.mjs'
      - 'scripts/verify-mobile-v2471.mjs'
      - 'scripts/release-mobile-v2471.sh'
      - 'patches/TAREFAS-2.4.6.8.tpatch'
      - '.github/workflows/android-v2471-alpha.yml'

concurrency:
  group: android-v2471-alpha
  cancel-in-progress: true

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'
      - name: Compilar, validar, assinar e publicar 2.4.7.1 Alpha
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          chmod +x scripts/release-mobile-v2471.sh
          scripts/release-mobile-v2471.sh
      - uses: actions/upload-artifact@v4
        with:
          name: tarefas-android-2.4.7.1-alpha-build-283
          path: |
            TAREFAS-2.4.7.1.apk
            TAREFAS-2.4.7.1.apk.sha256
            TAREFAS-2.4.7.1-alpha-build-283.zip
            TAREFAS-2.4.7.1-alpha-build-283.zip.sha256
            app/release-v2471.txt
            dist/ALPHA_2_4_7_1.json
          if-no-files-found: error
